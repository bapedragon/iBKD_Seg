# 저장소 작업 규칙

## 문서 언어

- README, ROADMAP, Phase별 결정문, 실행 안내, 보고서 안내 등 사용자용 문서는
  한국어로 작성합니다.
- 필요한 경우 영어 전문 용어를 함께 쓰되, 판단과 설명은 한국어로 전달합니다.
- 코드 식별자, CLI option, 파일명, machine-readable JSON key는 호환성을 위해
  영어를 유지할 수 있습니다.
- 새 문서를 추가하거나 기존 문서를 수정할 때 같은 언어 원칙을 적용합니다.

## 디렉터리 구조

- 연구 단계의 최상위 폴더는 `phase0/`, `phase1/`, `phase2/` 형식을 사용합니다.
- 각 Phase 폴더에는 해당 단계의 프로토콜, 실행 명령, 보고서, gate 결정을 둡니다.
- 여러 Phase가 공유하는 코드는 `src/ibkd_seg/`에 둡니다.
- 데이터셋, 체크포인트, 원시 실행 결과는 Git에 포함하지 않습니다.

## 재현성

- 공식 데이터와 체크포인트는 byte size와 SHA-256으로 식별합니다.
- 조건이 일치하지 않는 baseline은 주 비교 결과와 명확히 분리합니다.
- 실험 결과를 확인한 뒤 exclusion rule이나 평가 protocol을 임의로 바꾸지 않습니다.

## Phase 1 프로토콜 gate

- Phase 1 구현이나 실험을 시작하기 전에 미확정 프로토콜 항목을 먼저 점검하고,
  연구 결과에 영향을 줄 선택이 남아 있으면 사용자에게 이해하기 쉽게 알립니다.
- Phase 1A는 파이프라인 진단용이므로 구현 중 발견한 기술적 문제를 수정할 수 있지만,
  변경 내용과 이유를 기록하고 방법 간 순위를 최종 결론으로 사용하지 않습니다.
- Phase 1B H200 본실험 전에는 데이터 split과 경계 픽셀 처리, teacher와 encoder
  초기화, 공통 학습 설정, 방법별 고정값, checkpoint 선택, seed, probe, metric,
  test 사용 규칙을 문서와 설정 파일로 고정합니다.
- 위 항목 중 결과를 바꿀 수 있는 결정이 미확정이면 본실험을 임의로 시작하지 않습니다.
  먼저 선택지와 권장안을 제시하고 사용자의 결정을 확인합니다.
- 결과를 본 뒤 프로토콜을 바꿔야 한다면 기존 실행은 pilot으로 분리하고 버전을 올린 뒤,
  비교하는 모든 방법을 동일한 새 조건으로 다시 실행합니다.
