# 저장소 작업 규칙

## 문서 언어

- README, ROADMAP, Phase별 결정문, 실행 안내, 보고서 안내 등 사용자용 문서는
  한국어로 작성합니다.
- 필요한 경우 영어 전문 용어를 함께 쓰되, 판단과 설명은 한국어로 전달합니다.
- 코드 식별자, CLI option, 파일명, machine-readable JSON key는 호환성을 위해
  영어를 유지할 수 있습니다.
- 새 문서를 추가하거나 기존 문서를 수정할 때 같은 언어 원칙을 적용합니다.

## 디렉터리 구조

- 연구 단계의 최상위 폴더는 `phase0/`, `phase1/`, `phase2/` 형식을 사용합니다.
- 각 Phase 폴더에는 해당 단계의 프로토콜, 실행 명령, 보고서, gate 결정을 둡니다.
- 여러 Phase가 공유하는 코드는 `src/ibkd_seg/`에 둡니다.
- 데이터셋, 체크포인트, 원시 실행 결과는 Git에 포함하지 않습니다.

## 재현성

- 공식 데이터와 체크포인트는 byte size와 SHA-256으로 식별합니다.
- 조건이 일치하지 않는 baseline은 주 비교 결과와 명확히 분리합니다.
- 실험 결과를 확인한 뒤 exclusion rule이나 평가 protocol을 임의로 바꾸지 않습니다.
